document.addEventListener('DOMContentLoaded', () => {
    // Initialize modules
    initFileUpload();
    initFormHandlers();
    initPreview();
    initExport();
    initStorage();
    
    // Set initial date
    updateReportDate();
});

function updateReportDate() {
    // Date formatting logic from original code
}
